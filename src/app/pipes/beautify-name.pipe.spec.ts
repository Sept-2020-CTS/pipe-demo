import { BeautifyNamePipe } from './beautify-name.pipe';

describe('BeautifyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new BeautifyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
